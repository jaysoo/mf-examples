{
  "name": "react-rsbuild",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -F './*' --parallel dev",
    "build": "pnpm -F './*' build",
    "preview": "pnpm -F './*' --parallel preview",
    "test:e2e": "playwright test",
    "test:e2e:dynamic": "playwright test --config=playwright.dynamic.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0"
  }
}
